canvasapi==3.0.0
# Django 4.1.6 is available and works, but 4.1.* extended support ends in
# December 2023, whereas 3.2.* extended support ends in Apri 2024.
# See: https://www.djangoproject.com/download/#supported-versions
Django==3.2.17
mysqlclient==2.1.1
python-dotenv==0.21.1
# typing_extensions needed temporarily while using Python 3.10.  Had to
# downgrade to 3.10 because the OpenShift environment doesn't have a
# curated 3.11 image yet.
typing_extensions==4.5.0
